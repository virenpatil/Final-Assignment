function search(){
    // search using this text and find all the product display title on the links
    let searchText = $(this).val;
    productList.items.forEach(e => {
    if (displayName === searchText) {
        
    }
    });
}